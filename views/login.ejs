<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Mughal Molding | Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background: #1e293b; display: flex; align-items: center; justify-content: center; height: 100vh; margin: 0; }
        .lock-card { background: white; padding: 30px; border-radius: 15px; width: 350px; text-align: center; }
    </style>
</head>
<body>
    <div class="lock-card shadow-lg">
        <h4 class="fw-bold mb-4">Mughal Molding Center</h4>
        <p class="text-muted small">System is Locked</p>
        
        <% if (error) { %>
            <div class='alert alert-danger p-2 small'><%= error %></div>
        <% } %>

        <form method="POST" action="/login">
            <input type="password" name="password" class="form-control mb-3 text-center" placeholder="Enter Password" autofocus required>
            <button type="submit" class="btn btn-primary w-100">Unlock System</button>
        </form>
    </div>
</body>
</html>